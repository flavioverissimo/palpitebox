module.exports = {
  purge: [],
  theme: {
    extend: {},
    backgroundColor: (theme) => ({
      ...theme("colors"),
      colorLogo: "#44546A",
    }),
    textColor: (theme) => ({
      ...theme("colors"),
      colorLogo: "#44546A",
    }),
  },
  variants: {},
  plugins: [],
};
